{
  "ignoreCommand": "git fetch --depth=100 origin main 2>/dev/null; BASE=$(git merge-base HEAD origin/main 2>/dev/null || echo HEAD~1); git diff $BASE HEAD --name-only | grep -vE '^(BACKLOG\\.md$|BACKLOG/)' | grep -q . && exit 1 || exit 0"
}
